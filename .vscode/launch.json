{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to Firefox",
      "type": "firefox",
      "request": "attach",
      "url": "http://localhost:8080", // or the URL you are debugging
      "webRoot": "${workspaceFolder}"
    }
  ]
}
